## !!steps One

!Delay 0

```ts !
ipcMain.handle("GetPlayerData", async (event, ...args) =>
{

});
```

## !!steps Two

!Delay 0

```ts !
ipcMain.handle("GetPlayerData", async (event, ...args) =>
{
    const Player = args.length > 0;
});
```

## !!steps Three

!Delay 0

```ts !
ipcMain.handle("GetPlayerData", async (event, ...args) =>
{
    // const Player = args.length > 0;
});
```
