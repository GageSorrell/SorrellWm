# Reference

::: tip Purpose
This is the reference for `reactive-event`.
It was generated via [`typedoc`&nbsp;<FluentIcon Icon="ExternalLink" />](https://typedoc.org/) and [`typedoc-plugin-markdown` (via `typedoc-vitepress-theme`)&nbsp;<FluentIcon Icon="ExternalLink" />](https://typedoc-plugin-markdown.org/plugins/vitepress).
:::


The sections of the reference are described hereafter.

<div class="CustomTocContainer">
<p class="CustomTocTitle">In this article</p>

[[toc]]

</div>

## API

The content that is exported.
It is very likely that this is the only section that you will need to consult.

## Internal

The types used internally.
It is unlikely that you will need to directly interact with these types.


## Shared

The types used internally *and* by exported modules.
These are mostly foundational types, and it is unlikely that you will need to consult this part of the reference.
