# `npm init electron-reactive-event`

@TODO Write start of this, stating that this document specifically covers how to use the `init` command, and that *how* the actual code needed to set up `electron-reactive-event` with one's own project is in `./Setup.md`.

## Commands

There are three commands.
Two of the commands are described below; the third command `help` displays information mirroring what is given here.

### Preface: Arguments

The two main commands take the same arguments.

#### `--output`

|---    |---|-----|
| Value | Description | Example |


### `generate-preload`

### `generate-provider`
