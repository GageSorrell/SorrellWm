{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "description": "The format of the JSON config file for using the CLI commands\noffered by `electron-reactive-event`.",
    "properties": {
        "$schema": {
            "type": "string"
        },
        "AugmentationModulePath": {
            "description": "The path to the file containing the `declare module` block that augments\nthe `Registrar` type with your event declarations.  This path should be\nincluded in your `tsconfig.json`.",
            "type": "string"
        },
        "IpcModulePath": {
            "description": "`electron-reactive-event-cli` can create modules for you that export the reactive\nIPC functions, scoped to your `PackageKey`.",
            "properties": {
                "Main": {
                    "description": "The path of the module that calls `getReactiveIpcMain`, and exports its output.",
                    "type": "string"
                },
                "Renderer": {
                    "description": "The path of the module that calls `getReactiveIpcHooks`, and exports its output.",
                    "type": "string"
                }
            },
            "type": "object"
        },
        "PackageKey": {
            "description": "The unique identifier for your package.  This allows `electron-reactive-event`\nto be used by your package *and* any dependencies simultaneously.",
            "type": "string"
        },
        "ScopedModulePath": {
            "description": "The path to the file that exports types scoped to your `PackageKey`.\nEvery type exported by `electron-reactive-event/scoped` is imported here,\nand a new type of the same name is defined, with your `PackageKey` filled in\nfor the `PackageKey` type parameter.  This path should be included in your\n`tsconfig.json`.",
            "type": "string"
        }
    },
    "type": "object"
}

