# Setup

@TODO Write introduction explaining that this document explains what code is needed to set up `electron-reactive-event` with one's own project and how it can be done, and mention the `init` command and that its full documentation is in `./Init.md`, while still showing at the beginning of this document examples of using the `init` command to do the setup for you.

@TODO Then, explain how the code can be customized, and what is recommended.
